# Normalize line endings: store LF in git, checkout LF on all platforms
* text=auto eol=lf

# Windows-specific scripts get CRLF on checkout
*.bat text eol=crlf
*.ps1 text eol=crlf

# Binaries — no line ending conversion
*.exe binary
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.zip binary
